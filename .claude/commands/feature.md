Leia `claude.md` e `docs/claude/features.md` do projeto Cashly.

Feature solicitada: $ARGUMENTS

Com base na spec de "$ARGUMENTS" em `docs/claude/features.md`:

1. Mostre um resumo da feature (o que faz, qual persona se beneficia, qual sprint pertence)
2. Liste os arquivos que serão criados ou modificados (caminho exato)
3. Liste tipos novos que precisam ser adicionados em `src/types/index.ts`
4. Liste bibliotecas necessárias e se precisam ser instaladas
5. Mostre a ordem de implementação (tipagem → store → hook → componente → página → integração)

Confirme com o usuário e depois implemente na ordem acima, um passo de cada vez.

Ao concluir cada arquivo, informe brevemente o que foi feito e peça confirmação para continuar.
