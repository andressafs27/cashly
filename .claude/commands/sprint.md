Leia o arquivo `claude.md` e `docs/claude/features.md` do projeto Cashly.

Com base no sprint atual (indicado na tabela de sprints do claude.md como "próximo"), faça o seguinte:

1. Liste todas as entregas do sprint com seus arquivos alvo (caminho exato)
2. Indique quais já existem (mesmo que vazios) e quais precisam ser criados do zero
3. Mostre a ordem de implementação baseada em dependências (ex: store antes do hook, hook antes do componente, componente antes da página)
4. Para cada entrega, cite os tipos TypeScript relevantes de `src/types/index.ts`
5. Informe quais bibliotecas serão necessárias e se precisam ser instaladas

Depois pergunte: "Por qual item quer começar?"

Aguarde a resposta do usuário antes de implementar qualquer código.
